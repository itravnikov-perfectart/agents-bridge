{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "extension:build",
			"type": "shell",
			"command": "pnpm --filter agents-bridge-extension run build",
			"group": "build",
			"presentation": {
				"reveal": "silent"
			},
			"problemMatcher": "$tsc",
			"options": {
				"cwd": "${workspaceFolder}"
			}
		}
	]
}